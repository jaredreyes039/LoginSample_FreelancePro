const express = require('express');
const ROUTER = express.Router();
const USERS_CONTROLLER = require('../controllers/users.controller');

// ROUTER.post('/:user_id', USERS_CONTROLLER.getUserById)

exports.USERS = ROUTER;
